channels:
  global:
    prefix: "&b[Global] &r"
    format: "%prefix%&7%player%&f: %message%"
    range: -1
  local:
    prefix: "&e[Local] &r"
    format: "%prefix%&7%player%&f: %message%"
    range: 100